import { styled } from "baseui";

const Content = styled("div", {
  margin: "auto",
  display: "flex",
  flexWrap: "wrap",
  // maxWidth: "1024px",
  maxWidth: "64rem",
  justifyContent: "space-between"
});

export default Content;